{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

## BƯỚC 5: Tạo file .env.local (cho test local)

Tạo file `.env.local` (file này sẽ KHÔNG được push lên GitHub):
```
VITE_GEMINI_API_KEY=AIzaSy... (API key thật của bạn)
```

## BƯỚC 6: Cập nhật .gitignore

Kiểm tra file `.gitignore` đã có dòng này chưa (đã có rồi):
```
*.local
